{
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs && cp -r docs/.vitepress/dist/* dist/",
    "preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.0.1"
  }
}
