{
  "scripts": {
    "dev": "vitepress dev src",
    "build": "vitepress build src && cp -r src/.vitepress/dist/* docs/",
    "preview": "vitepress preview src"
  },
  "devDependencies": {
    "vitepress": "^1.0.1"
  }
}
